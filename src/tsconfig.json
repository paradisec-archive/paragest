{
  "extends": [
    "@tsconfig/node-lts/tsconfig.json",
    "@tsconfig/strictest/tsconfig.json",
  ],
  "compilerOptions": {
    "moduleResolution": "bundler",
    "lib": ["dom"] // for fetch remove after TS 5.3
  }
}
